# Tasks: 010-frontend-chat-integration

**Feature**: Frontend Chat Integration
**Spec**: [spec.md](./spec.md)
**Plan**: [plan.md](./plan.md)
**Date**: 2026-01-24

This task list is generated by `/sp.tasks` and is based on the design artifacts in this directory.

## Phase 1: Setup & Configuration

- [ ] T001 Verify that the Hugging Face Space URL is present and correct in `docusaurus.config.ts` under `customFields.chatApiUrl`.

## Phase 2: User Story 1 - Fix Backend API Integration

**Goal**: Update the frontend API client to correctly communicate with the production Hugging Face Gradio backend.
**Independent Test**: The chat widget, running on the local dev server, can successfully send a question to the live Hugging Face endpoint and display a valid response.

- [ ] T002 [US1] Update the `useChatApiUrl` hook in `ros2-textbook/src/components/Chat/apiClient.ts` to correctly read the `chatApiUrl` from Docusaurus's `siteConfig.customFields`.
- [ ] T003 [US1] Modify the `fetch` call in `ros2-textbook/src/components/Chat/apiClient.ts` to target the Hugging Face Gradio endpoint suffix, which is `/run/predict`.
- [ ] T004 [US1] Adapt the request payload in `ros2-textbook/src/components/Chat/apiClient.ts` to match the required Gradio format: `{ "data": [question] }`.
- [ ] T005 [US1] Update the response handling logic in `ros2-textbook/src/components/Chat/apiClient.ts` to unwrap the `ChatResponsePayload` from the nested `json.data[0]` structure and parse the inner JSON string.

## Phase 3: Verification

**Goal**: Ensure the integration is working as expected and is ready for deployment.
**Independent Test**: The chat widget works end-to-end on the local development server without any console errors.

- [ ] T006 [P] Manually test the chat widget on the local development server (`npm start`) to ensure it communicates with the Hugging Face backend and correctly displays success, system, refused, and error statuses.
- [ ] T007 [P] Review all code changes for quality and clarity, ensuring no console errors are present during operation.

## Dependencies

- **User Story 1** is the only user story in scope for this task list and has no dependencies on other user stories.

## Parallel Execution

- Within Phase 3, tasks T006 and T007 can be performed in parallel.

## Implementation Strategy

The strategy is to complete the tasks sequentially as laid out, focusing on modifying the `apiClient.ts` to meet the specific contract of the deployed Gradio backend. The MVP is a functional chat widget on the local development environment that can successfully communicate with the production backend.